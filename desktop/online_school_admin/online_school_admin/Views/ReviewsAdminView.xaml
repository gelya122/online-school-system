<UserControl x:Class="online_school_admin.Views.ReviewsAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock FontSize="22" FontWeight="SemiBold" Foreground="#111827" Text="Отзывы" />
            <Button Margin="16,0,0,0" Padding="12,6" Content="Назад" Command="{Binding BackCommand}" />
            <Button Margin="8,0,0,0" Padding="12,6" Content="Обновить" Command="{Binding RefreshCommand}" />
            <Button Margin="8,0,0,0" Padding="12,6" Content="Опубликовать" Command="{Binding PublishCommand}" />
            <Button Margin="8,0,0,0" Padding="12,6" Content="Скрыть" Command="{Binding HideCommand}" />
        </StackPanel>

        <TextBlock DockPanel.Dock="Top" Margin="0,0,0,8" Foreground="#C0392B" Text="{Binding Error}" TextWrapping="Wrap" />

        <DataGrid ItemsSource="{Binding Rows}"
                  SelectedItem="{Binding Selected}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  RowHeaderWidth="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Студент" Binding="{Binding StudentName}" Width="160" />
                <DataGridTextColumn Header="Курс" Binding="{Binding CourseTitle}" Width="200" />
                <DataGridTextColumn Header="Оценка" Binding="{Binding Rating}" Width="70" />
                <DataGridTextColumn Header="Текст" Binding="{Binding Comment}" Width="*" />
                <DataGridCheckBoxColumn Header="На сайте" Binding="{Binding IsPublished}" Width="90" />
                <DataGridTextColumn Header="Создан" Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy}" Width="110" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
