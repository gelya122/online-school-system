<Window x:Class="online_school_admin.Views.AssignApplicationManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Назначить менеджера"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="Height"
        Width="480">
    <StackPanel Margin="16">
        <TextBlock Text="Клиент" Foreground="#6B7280" />
        <TextBlock x:Name="ClientNameBlock" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,2,0,12" />

        <TextBlock Text="Текущий менеджер" Foreground="#6B7280" />
        <TextBlock x:Name="CurrentManagerBlock" Margin="0,2,0,12" />

        <TextBlock Text="Менеджер по продажам" Foreground="#6B7280" />
        <ComboBox x:Name="ManagerCombo" DisplayMemberPath="Title" Margin="0,4,0,12" Height="28" />

        <TextBlock Text="Комментарий (в ленту заявки)" Foreground="#6B7280" />
        <TextBox x:Name="NoteBox" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="72" Margin="0,4,0,12" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" IsCancel="True" Width="100" Margin="0,0,8,0" Padding="10,6" Click="Cancel_Click" />
            <Button Content="Назначить" IsDefault="True" Width="110" Padding="10,6" Click="Ok_Click" />
        </StackPanel>
    </StackPanel>
</Window>
