<UserControl x:Class="online_school_admin.Views.FaqAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock FontSize="22" FontWeight="SemiBold" Foreground="#111827" Text="FAQ" />
            <Button Margin="16,0,0,0" Padding="12,6" Content="Назад" Command="{Binding BackCommand}" />
            <Button Margin="8,0,0,0" Padding="12,6" Content="Обновить" Command="{Binding RefreshCommand}" />
        </StackPanel>

        <TextBlock DockPanel.Dock="Top" Margin="0,0,0,8" Foreground="#C0392B" Text="{Binding Error}" TextWrapping="Wrap" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <TextBlock DockPanel.Dock="Top" FontWeight="SemiBold" Margin="0,0,0,8" Text="Категории" />
                <StackPanel DockPanel.Dock="Bottom" Margin="0,12,0,0">
                    <TextBox Padding="8,6" Margin="0,0,0,8" Text="{Binding NewCategoryName, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Padding="12,6" Content="Добавить категорию" Command="{Binding AddCategoryCommand}" />
                    <Button Margin="0,8,0,0" Padding="12,6" Content="Удалить категорию" Command="{Binding DeleteCategoryCommand}" />
                </StackPanel>
                <ListBox ItemsSource="{Binding Categories}"
                         SelectedItem="{Binding SelectedCategory}"
                         DisplayMemberPath="CategoryName" />
            </DockPanel>

            <DockPanel Grid.Column="2">
                <TextBlock DockPanel.Dock="Top" FontWeight="SemiBold" Margin="0,0,0,8" Text="Вопросы выбранной категории" />
                <StackPanel DockPanel.Dock="Bottom" Margin="0,12,0,0">
                    <Button HorizontalAlignment="Left" Padding="12,6" Content="Новый вопрос" Command="{Binding NewItemCommand}" />
                    <TextBlock Margin="0,12,0,4" Text="Вопрос" />
                    <TextBox Padding="8,6" Text="{Binding ItemQuestion, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Margin="0,8,0,4" Text="Ответ" />
                    <TextBox Padding="8,6" MinHeight="80" TextWrapping="Wrap" AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Text="{Binding ItemAnswer, UpdateSourceTrigger=PropertyChanged}" />
                    <WrapPanel Margin="0,8,0,0">
                        <TextBlock VerticalAlignment="Center" Text="Порядок" />
                        <TextBox Margin="8,0,0,0" Width="80" Padding="8,6" Text="{Binding ItemOrderText, UpdateSourceTrigger=PropertyChanged}" />
                        <CheckBox Margin="16,0,0,0" VerticalAlignment="Center" Content="Активен" IsChecked="{Binding ItemIsActive}" />
                    </WrapPanel>
                    <StackPanel Margin="0,12,0,0" Orientation="Horizontal">
                        <Button Padding="12,6" Content="Сохранить вопрос" Command="{Binding SaveItemCommand}" />
                        <Button Margin="8,0,0,0" Padding="12,6" Content="Удалить вопрос" Command="{Binding DeleteItemCommand}" />
                    </StackPanel>
                </StackPanel>
                <DataGrid ItemsSource="{Binding Items}"
                          SelectedItem="{Binding SelectedItem}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          RowHeaderWidth="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Вопрос" Binding="{Binding Question}" Width="*" />
                        <DataGridCheckBoxColumn Header="Активен" Binding="{Binding IsActive}" Width="80" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Grid>
    </DockPanel>
</UserControl>
